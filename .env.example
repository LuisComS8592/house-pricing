# Copy this file to ".env" and fill in your own values.
# ".env" is git-ignored and must never be committed.

POSTGRES_USER=admin
POSTGRES_PASSWORD=change_me
POSTGRES_DB=house_pricing

# Must stay consistent with the three variables above.
DATABASE_URL=postgresql://admin:change_me@db:5432/house_pricing
